spring:
  rabbitmq:
    addresses: amqp://localhost:55672
    username: coffeehouse
    password: password

server:
  error:
    include-binding-errors: always
    include-exception: true
    include-message: always
    include-stacktrace: always

logging:
  level:
    application: DEBUG
    module: DEBUG
    library: DEBUG
    web: DEBUG
    sql: DEBUG
